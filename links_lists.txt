find all bottom of node
  where


new node
  new list
  new item

new link
  ? source is root & target is root
    (merge target list into source list)
    target.list.items { depth + 1, list = source.list }
    delete target.list
  ? source is root && target is not root
    (duplicate end of one of target lists)
    target.list.items


find parents

(SELECT lli0."nodeId" FROM
"links_lists" as ll0,
"links_lists_items" as lli0
WHERE
ll0."nodeId" = 4 AND
lli0."listId" = ll0."id")